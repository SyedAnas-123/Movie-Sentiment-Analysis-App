services:
  - type: web
    name: movie-sentiment-analysis-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py
    pythonVersion: 3.10
